{
  "name": "changesets-issue-repro",
  "version": "0.0.1",
  "scripts": {
    "publish": "pnpm build && changeset publish"
  },
  "dependencies": {},
  "devDependencies": {
    "@changesets/cli": "^2.26.1"
  }
}
